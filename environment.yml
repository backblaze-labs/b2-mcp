name: b2-mcp
channels:
  - conda-forge
dependencies:
  - nodejs=22.23.1
  # Python is present only for conda-forge trufflehog 2.2.1, which requires
  # Python <3.11. Keep this pin with the scanner pin below.
  - python=3.10.20
  - git=2.55.0
  - gh=2.96.0
  - trufflehog=2.2.1
