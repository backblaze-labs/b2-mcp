version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
      semver-major-days: 7
      semver-minor-days: 3
      semver-patch-days: 3
    open-pull-requests-limit: 5
    groups:
      # The SDK will handle durable B2 credentials after migration. Every bump
      # requires human parity, provenance, and engine-floor review.
      b2-sdk:
        patterns:
          - "@backblaze-labs/b2-sdk"
        update-types:
          - minor
          - patch
      # TOON dev/test decoder bumps can change contract-oracle behavior. Keep
      # them isolated from routine dependency drift.
      toon-format:
        patterns:
          - "@toon-format/toon"
        update-types:
          - minor
          - patch
      aws-sdk:
        patterns:
          - "@aws-sdk/*"
        update-types:
          - minor
          - patch
      dev-dependencies:
        dependency-type: development
        # Keep development drift reviewable; majors stay as separate PRs.
        update-types:
          - minor
          - patch
    labels:
      - dependencies
    ignore:
      # These majors can move the effective runtime floor or contract oracle.
      # Review them manually outside routine dependency refreshes.
      - dependency-name: opossum
        update-types: ["version-update:semver-major"]
      - dependency-name: "@types/node"
        update-types: ["version-update:semver-major"]
      - dependency-name: "@toon-format/toon"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
    groups:
      github-actions-minor-patch:
        patterns:
          - "*"
        update-types:
          - minor
          - patch
    labels:
      - dependencies
      - github-actions
